<?php

return [
    
    |--------------------------------------------------------------------------
    | Оптимизация запросов
    |--------------------------------------------------------------------------
    |
    | Включение или отключение автоматической оптимизации запросов к базе данных.
    | При включенной опции запросы будут оптимизированы для использования индексов
    | и минимизации нагрузки на базу данных.
    |

    'optimizeQueries' => env('OPTIMIZER_QUERIES', true),

    
    |--------------------------------------------------------------------------
    | Кеширование
    |--------------------------------------------------------------------------
    |
    | Включение или отключение автоматического кеширования результатов запросов.
    | При включенной опции результаты запросов будут сохраняться в кеше для
    | повышения производительности приложения.
    |

    'enableCaching' => env('OPTIMIZER_CACHING', true),

    
    |--------------------------------------------------------------------------
    | Время жизни кеша
    |--------------------------------------------------------------------------
    |
    | Время хранения кешированных данных в минутах. По умолчанию данные
    | хранятся 60 минут, после чего они будут автоматически обновлены.
    |

    'defaultCacheLifetime' => env('OPTIMIZER_CACHE_LIFETIME', 60),

    
    |--------------------------------------------------------------------------
    | Кеширование запросов
    |--------------------------------------------------------------------------
    |
    | Настройки кеширования для различных типов запросов.
    | Вы можете задать разное время кеширования для разных типов данных.
    |

    'cacheSettings' => [
        
        'models' => [
            'user' => 120, 
            'ticket' => 30, 
            'equipment' => 240, 
            'knowledgeBase' => 1440, 
        ],

        
        'api' => [
            'tickets' => 15, 
            'users' => 30, 
            'statistics' => 60, 
        ],

        
        'views' => [
            'homepage' => 30, 
            'knowledgeBase' => 60, 
            'static' => 1440, 
        ],
    ],

    
    |--------------------------------------------------------------------------
    | Исключения для кеширования
    |--------------------------------------------------------------------------
    |
    | Списки моделей, роутов и шаблонов, которые не должны кешироваться.
    | Используйте это для динамического контента, который часто меняется.
    |

    'exclude' => [
        
        'models' => [
            'Notification',
            'Log',
        ],

        
        'api' => [
            'notifications',
            'live-data',
        ],

        
        'views' => [
            'dashboard.realtime',
            'user.profile',
        ],
    ],

    
    |--------------------------------------------------------------------------
    | Автоматическая инвалидация кеша
    |--------------------------------------------------------------------------
    |
    | При включенной опции кеш будет автоматически сбрасываться при изменении
    | связанных данных. Например, при изменении пользователя будет сброшен кеш
    | всех страниц, связанных с этим пользователем.
    |

    'autoInvalidation' => env('OPTIMIZER_AUTO_INVALIDATION', true),

    
    |--------------------------------------------------------------------------
    | Сжатие ответов
    |--------------------------------------------------------------------------
    |
    | При включенной опции ответы сервера будут автоматически сжиматься
    | для уменьшения размера передаваемых данных. Рекомендуется включать
    | эту опцию для экономии трафика и повышения скорости загрузки страниц.
    |

    'compressResponses' => env('OPTIMIZER_COMPRESS_RESPONSES', true),

    
    |--------------------------------------------------------------------------
    | Оптимизация изображений
    |--------------------------------------------------------------------------
    |
    | При включенной опции загружаемые изображения будут автоматически
    | оптимизироваться для уменьшения их размера без значительной потери качества.
    |

    'optimizeImages' => env('OPTIMIZER_IMAGES', true),

    
    |--------------------------------------------------------------------------
    | Отложенная загрузка изображений
    |--------------------------------------------------------------------------
    |
    | При включенной опции изображения будут загружаться с использованием
    | техники "lazy loading", что ускоряет начальную загрузку страницы.
    |

    'lazyLoadImages' => env('OPTIMIZER_LAZY_LOAD', true),
];
